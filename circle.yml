machine:
  node:
    version: 8.1.2
  python:
    version: 3.4.2

dependencies:
  pre:
    - createdb -u ubuntu fstool_e2etest
  post:
    - bash setup_frontend.sh
    - bash setup_backend.sh
    - bash setup_webdriver.sh
