machine:
  node:
    version: 8.1.2
  python:
    version: 3.5.3
  services:
    - redis

dependencies:
  pre:
    - createdb fstool_e2etest
  post:
    - bash setup_frontend.sh
    - bash setup_backend.sh
    - bash setup_webdriver.sh
    - wget --retry-connrefused --no-check-certificate -T 30 http://localhost:4444
